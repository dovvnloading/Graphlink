# Plugin System Refactor Plan

**Project:** Graphite App
**Date:** 2026-07-07
**Status:** Phases 0-5 complete. Follow-on work beyond the original phased plan: the 7-plugin single-shared-worker-thread bug fixed (§7), `CodeReviewAnalyzer`'s (§4.2), `QualityGateAnalyzer`'s (§4.6), and Gitlink's `GitlinkAgent`'s (§4.4) Qt-adjacent logic each extracted into their own Qt-free modules — deliberately kept as separate modules per plugin, not merged into one shared engine (see §4.2/§4.6/§4.4 for why). Gitlink's extraction also surfaced a real scope correction: `build_context_bundle`/`_resolve_scope_paths`/`_scan_local_repo_paths` turned out to be `GitlinkNode` instance methods entangled with widget state, not free functions as originally assumed, so they deliberately stayed put (§4.4). `PluginPortal.execute_plugin`'s not-found case now shows a notification banner instead of only `print()`ing (§1.1/§4.7/§4.8), and Workflow's `SYSTEM_PROMPT` "Allowed plugins:" bullet list is now generated from `WORKFLOW_ALLOWED_PLUGINS` instead of hand-typed a second time, structurally preventing the exact drift that already caused a live bug once (§4.7). Remaining deliberately-out-of-scope items noted below. Fixed: Workflow allowlist drift for Code Review Agent (§1.4/§4.7), Gitlink's write-approval state machine + path-safety tests (§2.2/§4.4), Code Sandbox's human-approval gate (§2.1/§4.3), the additive `PluginSpec`/`PLUGIN_REGISTRY` table (§3.1/§4.8), the `create_node()` generic factory now used by 11 of 13 plugins (§3.2/§4.1/§4.8), the `seed_prompt()` protocol replacing the isinstance dispatch chain for all 11 seedable plugins (§3.4/§4.7), Graph Diff's and Quality Gate's node-label maps migrated onto the registry — fixing a real missing-`CodeReviewNode` display bug in Graph Diff's case (§4.5/§4.6) — the shared `GitHubRestClient` (§1.6/§4.2/§4.4) and `extract_json_object`/`call_llm_and_parse_json` helpers (§1.6/§4.2/§4.6) replacing five independent copies of duplicated networking/JSON boilerplate, the HTML Renderer/`CodeNode` crash fixed after a dedicated deletion-cascade investigation confirmed it was safe (§4.8/§5), the dual import paths resolved and all 9 root shim files deleted (§1.5), `graphite_plugin_context_menu.py` moved into the package — which surfaced and fixed a real circular import in `graphite_plugins/__init__.py`'s eager re-exports (§4.9/§5) — and five `graphite_scene.py` methods deduplicated onto pre-existing-but-underused `_all_conversational_nodes()`/`_all_content_nodes()`/`_all_layout_nodes()` helpers (§3.3/§5). 216 tests now cover this work (`graphite_app/tests/`). Still open, deliberately: Code Sandbox OS-level hardening (needs a scope decision on adding dependencies), Quality Gate's/Workflow's icon maps (unifying them would visibly change the recommendation-card UI, not just refactor plumbing), the deletion isinstance chain in `graphite_scene.py`/type dispatch chains in `graphite_window.py` (investigated in depth — real per-type differences in dispose/thread-cleanup/GraphDiff's bespoke path make full unification a correctness risk not worth the mechanical gain, see §5), each analyzer's actual scoring/rubric *design* (the point-value heuristics themselves, not their location in the codebase), Gitlink's `build_context_bundle` (still a `GitlinkNode` method, by design — see §4.4), and Workflow's `_normalize_plan` silent-drop behavior/`WorkflowArchitectAgent` extraction (§4.7, still not done unlike its three sibling analyzers).

## Scope & Method

"Plugins" in this codebase are the 9 node types registered in [graphite_plugin_portal.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_portal.py): Artifact/Drafter, Code Review Agent, Execution Sandbox (Code Sandbox), Gitlink, Branch Lens (Graph Diff), Quality Gate, Workflow Architect, plus the meta-infrastructure that hosts them (Plugin Portal, Plugin Flyout/Picker, the shared context menu). Their real implementations live in `graphite_app/graphite_plugins/*.py` (400–2300 lines each, ~10,000 lines total).

This review read every plugin implementation in full, plus every integration point that wires a plugin into the rest of the app (`graphite_scene.py`, `graphite_window.py`, `graphite_window_actions.py`, `graphite_session/{serializers,deserializers,scene_index}.py`, `graphite_command_palette.py`). Findings were independently gathered per plugin and cross-checked against the source; specific line numbers below were re-verified directly, not taken on faith from a single pass.

## Executive Summary

The complaint that the plugins are "extremely weak and brittle" is accurate, but the root cause isn't any one plugin's code quality — it's that **there is no plugin system**. `PluginPortal` documents itself as "a centralized manager for discovering, listing, and executing available plugins," but discovery is 13 hardcoded method calls, listing is a hardcoded category table, and execution is a linear string match. Every plugin's node type is registered by hand in at least **7 separate files**, using near-identical boilerplate, with no shared base class, no interface, and nothing that fails loudly if a spot is missed. One instance of that drift was live in the app until this review (§1.4/§4.7) — now fixed.

On top of the systemic problem, three plugins carried their own serious issues: **Code Sandbox** executes arbitrary code with no OS-level sandboxing and installs unpinned/unverified pip packages (§2.1 — now gated behind an explicit human-approval checkpoint; real OS-level hardening is still open pending a scope decision); **Gitlink**'s "only writes after explicit approval" guarantee was enforced by a single dialog box, not a verifiable state (§2.2 — now fixed with a fingerprinted state machine); and **Code Review** (2273 lines) and **Gitlink** (1884 lines) each hand-roll their own GitHub REST client, while **Code Review** and **Quality Gate** each hand-roll their own LLM-JSON scoring-agent skeleton — four different files independently reimplementing two things (still open, tracked in §4.2/§4.4/§4.6).

This document lays out the systemic root cause, two priority fixes that shouldn't wait for a full refactor, a proposed target architecture, a refactor plan for each of the 9 plugins individually, and a suggested phased sequence for doing the work without a big-bang rewrite.

---

## 1. Root Cause: There Is No Plugin System

### 1.1 `PluginPortal` is a lookup table, not a registry

[graphite_plugin_portal.py:70-173](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_portal.py#L70) (`_discover_plugins`) calls `_register_plugin(name=..., description=..., callback=..., category=..., icon=...)` thirteen times, by hand. Each `callback` is a hand-written `_create_X_node` method (e.g. `_create_quality_gate_node`, [graphite_plugin_portal.py:391-417](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_portal.py#L391)) that repeats the same ~15-line shape for every plugin: resolve the parent node → construct `XNode(...)` → append to `parent_node.children` → connect 1-3 Qt signals to hardcoded `main_window.execute_X_node` methods → position the node → `scene.addItem(node)` → append to a hardcoded per-type list (`scene.quality_gate_nodes`) → construct `XConnectionItem(parent, node)` → append to another hardcoded list (`scene.quality_gate_connections`).

~~`execute_plugin` ([graphite_plugin_portal.py:205-210](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_portal.py#L205)) is a linear scan for a matching `name` string; on miss it `print()`s a warning and returns `None` — no exception, no typed failure, callers can silently no-op (confirmed in practice, see §1.4/§4.7).~~ **✅ Fixed** — still a linear scan (that part is fine; 13 entries, called on user action, not a hot path), but the not-found case now calls `self.main_window.notification_banner.show_message(...)` with a `"warning"` level instead of `print()`, so `instantiate_seeded_plugin`'s existing "restore state and return on `None`" path is no longer silent to the user. Kept the return value as `None` (not a typed result/exception) — every other failure path in this same file (`no_selection_message`/`invalid_parent_message` in `create_node()`) already uses "show a banner, return `None`", so this matches the codebase's established idiom instead of introducing a second, inconsistent failure-signaling convention. See §4.7/§4.8.

### 1.2 The seven-file tax

Adding, renaming, or removing a plugin type requires synchronized hand-edits across at least these files, each with its own copy of the same knowledge:

| File | What it duplicates per plugin type |
|---|---|
| [graphite_plugin_portal.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_portal.py) | registration entry + `_create_X_node` factory method |
| [graphite_scene.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_scene.py) | `self.X_nodes = []` / `self.X_connections = []` lists ([lines 58-94](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_scene.py#L58)), plus the identical `self.artifact_nodes + self.workflow_nodes + self.graph_diff_nodes + self.quality_gate_nodes + self.code_review_nodes + self.gitlink_nodes` concatenation **repeated verbatim 6 times** (lines 243, 309, 335, 520, 718, 941) and per-type removal branches (e.g. line 1374, 1418) |
| [graphite_window.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_window.py) | `isinstance()` checks + a hardcoded display-name string per node type |
| [graphite_window_actions.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_window_actions.py) | `execute_X_node` method + `XWorkerThread` construction + cleanup/error handlers |
| [graphite_session/serializers.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_session/serializers.py) | per-type serialize branch |
| [graphite_session/deserializers.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_session/deserializers.py) | per-type deserialize branch |
| [graphite_session/scene_index.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_session/scene_index.py) | per-type index entry |

A grep of any given plugin class name against these files returns near-identical counts across all 7 plugin types (7 hits in `graphite_scene.py`, 2-3 in each session file, 3-9 in window/window_actions) — strong evidence this is copy-paste-driven development, not organic per-plugin variation.

### 1.3 The same "which node type is this" knowledge is hand-copied 3-4 times over

- `graphite_plugin_portal.py`'s registration calls (name strings, [lines 71-173](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_portal.py#L71))
- `graphite_plugin_quality_gate.py`'s `_node_label` class-name → display-name map, all 13 entries hardcoded ([graphite_plugin_quality_gate.py:124-140](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_quality_gate.py#L124))
- `graphite_plugin_graph_diff.py`'s own equivalent node-label map
- `graphite_plugin_workflow.py`'s `WORKFLOW_PLUGIN_ICONS` / `WORKFLOW_ALLOWED_PLUGINS` ([graphite_plugin_workflow.py:30-43](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_workflow.py#L30)) plus a third copy embedded in that plugin's LLM system prompt text

None of these derive from the portal's registration table. They're independent, hand-maintained lists that must agree by pure string equality.

### 1.4 Confirmed live bug caused by this drift — ✅ Fixed for Code Review Agent

`WORKFLOW_ALLOWED_PLUGINS` ([graphite_plugin_workflow.py:30-43](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_workflow.py#L30)) was checked directly: it listed 9 plugin names and omitted both "Branch Lens" and "Code Review Agent" — both of which are live, registered plugins in `graphite_plugin_portal.py` ([lines 104, 120](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_portal.py#L104)). Because `_normalize_plan` silently drops any LLM-recommended plugin name not in that allowlist, the Workflow Architect could never recommend or seed either node, with no error, log, or user-visible indication that anything was filtered.

On closer inspection the two omissions are **not symmetric**:
- **Code Review Agent** fits the standard single-parent-plus-starter-prompt seeding contract exactly like every other seedable plugin (it's already handled in `_seed_plugin_prompt`'s isinstance chain, [graphite_window_actions.py:1675-1677](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_window_actions.py#L1675)). Its omission was a pure drift bug. **Fixed**: added to `WORKFLOW_PLUGIN_ICONS`, the system-prompt's allowed-plugins list and usage rule, and the `_fallback_plan` keyword heuristics in `graphite_plugin_workflow.py`.
- **Branch Lens** genuinely cannot be seeded the same way: `PluginPortal._create_graph_diff_node` ([graphite_plugin_portal.py:483-513](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_portal.py#L483)) ignores `main_window.current_node` entirely and instead requires exactly two matching node types already selected in the scene (`len(selected_nodes) != 2`). Adding it to the workflow allowlist as-is would not fix anything — it would replace a silent no-op with a confusing "Select exactly two branch-tip nodes" warning when a user clicks "Add Node" after a recommendation they can't act on the same way as the others. **Left excluded intentionally.** A real fix requires teaching the seeding flow about plugins with a "requires N pre-selected nodes" contract instead of the single-parent one — tracked as new work in §4.7's refactor plan rather than a one-line allowlist fix.

This is still exactly the failure mode the missing registry predicts (§1.1): a hand-maintained list drifted out of sync with reality, and nothing caught it until this review.

### 1.5 Half-finished package migration (dual import paths) — ✅ Fixed

Every plugin had a root-level compatibility shim, e.g. `graphite_plugin_artifact.py` (9 lines, `from graphite_plugins.graphite_plugin_artifact import (...)`), pointing at the real implementation in `graphite_plugins/`. The core app files (`graphite_scene.py`, `graphite_window.py`, `graphite_window_actions.py`, `graphite_window_navigation.py`, all three `graphite_session/*.py` files) imported via the **old root shim path**, while `graphite_plugin_portal.py` itself imported directly via the **new package path**. Both resolved to the same classes, so nothing was broken, but it meant the "real" package boundary was only half-adopted.

**Fixed:** all 7 core files now import via `graphite_plugins.graphite_plugin_X`, including a deferred import inside `graphite_window_actions.py` (`from graphite_plugin_artifact import ArtifactWorkerThread`, hidden inside a function body — an anchored `^from ...` grep sweep misses these, worth remembering for any future import-path audit). The 9 now-unused root shim files (`graphite_plugin_artifact.py`, `graphite_plugin_code_review.py`, `graphite_plugin_code_sandbox.py`, `graphite_plugin_context_menu.py`, `graphite_plugin_gitlink.py`, `graphite_plugin_graph_diff.py`, `graphite_plugin_picker.py`, `graphite_plugin_portal.py`, `graphite_plugin_quality_gate.py`, `graphite_plugin_workflow.py` — note `graphite_plugin_context_menu.py` was the real implementation, not a shim, and was moved rather than deleted, see §4.9) were deleted. Verified via full app import-chain checks and the full test suite before and after deletion. Covered by [tests/test_import_chain.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/tests/test_import_chain.py).

### 1.6 Duplicated engines across plugins

- **GitHub REST client** — ✅ **Fixed (Phase 3a).** `graphite_plugin_code_review.py`'s `_get_github_token`/`_github_headers`/`_github_request` was near-identical to `graphite_plugin_gitlink.py`'s equivalent — the same token retrieval, header construction, and HTTP-status-to-error-message mapping hand-rolled twice. Extracted into [graphite_plugins/common/github_client.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/common/github_client.py) (`GitHubRestClient`). Both `CodeReviewNode` and `GitlinkNode` now construct `self._github_client = GitHubRestClient(settings_manager)` and their `_get_github_token`/`_github_headers`/`_github_request` methods are thin delegating wrappers — kept as wrappers (not removed) so every existing call site across both ~2000-line files stays unchanged, which is what keeps this a low-risk mechanical extraction rather than a rewrite. The higher-level, per-plugin methods that build on this (`load_github_repositories`, `_resolve_repo_and_branch`, tree/file loading — these have real per-plugin UI side effects, not just duplicated networking) were deliberately left alone; only the byte-for-byte duplicated boilerplate was extracted. Covered by [tests/test_github_client.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/tests/test_github_client.py) (client logic in isolation: token/header construction, 404/401/403-rate-limit/other-error/success/expect_json=False paths) and [tests/test_github_client_node_delegation.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/tests/test_github_client_node_delegation.py) (guards against either node class silently reverting to its own hand-rolled logic).
- **LLM-JSON scoring-agent skeleton** — ✅ **Partially fixed (Phase 3b).** `CodeReviewAnalyzer`, `QualityGateAnalyzer`, and (a fourth copy discovered during this phase) `WorkflowArchitectAgent` all independently hand-rolled the identical JSON-fence-stripping regex, and `CodeReviewAnalyzer`/`QualityGateAnalyzer`/`WorkflowArchitectAgent` also independently hand-rolled the identical `api_provider.chat(...)` → `json.loads(...)` boilerplate around it (`GitlinkAgent` has the regex too, but a genuinely different call shape — see below). Extracted into [graphite_plugins/common/llm_json.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/common/llm_json.py): `extract_json_object(raw_text)` (the regex, used by all four) and `call_llm_and_parse_json(system_prompt, user_prompt, *, task)` (the network-call-then-parse boilerplate, used by the three that share that shape). This is deliberately **plain functions, not a shared base class with a `get_response()` template method** as originally proposed in §3.5 — the three plugins turned out to differ in exactly where fallback/normalization happens relative to the try/except (`CodeReviewAnalyzer` normalizes *outside* the try, `QualityGateAnalyzer`/`WorkflowArchitectAgent` normalize *inside* it, so a JSON-valid-but-un-normalizable response also triggers their fallback but not Code Review's), so each plugin keeps its own try/except/fallback control flow and just calls the shared functions instead of duplicating their bodies — forcing that difference into one template method would have meant a parameterized hook for one exception case, which isn't worth it for three call sites. Each plugin's own `_extract_json`/`_clean_json_response` method (and Gitlink's module-level `_extract_json_object` function) is kept as a one-line delegating wrapper so every existing call site is unchanged. `GitlinkAgent.get_response` was **not** migrated onto `call_llm_and_parse_json`: its network call is deliberately outside the try/except entirely (only `json.loads` is guarded, and only against `json.JSONDecodeError` specifically) — a real, pre-existing behavioral difference from the other three, not something to paper over. Covered by [tests/test_llm_json.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/tests/test_llm_json.py) (the shared functions in isolation) and [tests/test_llm_json_plugin_delegation.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/tests/test_llm_json_plugin_delegation.py) (all four plugins' delegation, plus that the fallback path still fires correctly on exceptions for the three that share it). **Update:** each analyzer's scoring engine has since been physically separated out of its Qt-heavy widget file (`CodeReviewAnalyzer` into `graphite_plugins/code_review/scoring.py`, see §4.2) — but the two rubrics remain deliberately un-merged with each other, since their weights/categories/fallback heuristics are genuinely different domain logic, not incidental duplication.

### 1.7 QThread-per-feature: cooperative cancellation that can't actually cancel

Every plugin's `XWorkerThread.stop()` (Artifact, Code Review, Gitlink, Graph Diff, Quality Gate, Workflow — 6 of 7 worker threads checked) only flips an `_is_running` flag. None of them can interrupt an in-flight blocking call (`api_provider.chat`, `requests.get`) already in progress inside `run()` — `stop()` just suppresses the result once the blocking call eventually returns. This is a systemic, codebase-wide convention (also present outside plugins, e.g. `graphite_agents_core.py`), not a defect unique to any one plugin, but every plugin inherits it. Code Sandbox is the one exception with genuine subprocess-level cancellation, because it was already hardened in a prior bug sweep ([BUG_SWEEP_REPORT.md](C:/Users/Admin/source/repos/graphite_app/doc/BUG_SWEEP_REPORT.md) item 3).

### 1.8 "Agents" vs. "plugins" boundary is inconsistent

Code Sandbox's actual execution engine (`VirtualEnvSandbox`, `CodeSandboxExecutionWorker`, LLM generation/repair agents) lives in root-level `graphite_agents_code_sandbox.py`, not in `graphite_plugins/`, even though Code Sandbox is registered and treated as a plugin everywhere else. Every other plugin is self-contained inside its `graphite_plugins/graphite_plugin_X.py` file.

---

## 2. Priority Fixes (independent of the broader refactor)

These two items are security/data-safety relevant and cheap to isolate — recommend doing them before or alongside the architectural work below, not after it.

### 2.1 Code Sandbox: unconfined execution + unverified installs — ✅ Approval gate added (OS-level hardening still open)

[graphite_agents_code_sandbox.py:294-307](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_agents_code_sandbox.py#L294) runs AI/user-generated Python via `subprocess.Popen` with the full privileges of the host OS user — a venv isolates the Python package graph, not the OS (no container, no seccomp/Job Object, no filesystem/network restriction). [Lines 254-292](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_agents_code_sandbox.py#L254) run `pip install -r requirements.txt` with no `--require-hashes`, no version pinning enforcement, and no package allowlist — any package name a user or the generation LLM writes is fetched from PyPI and its build hooks executed, unverified. Timeouts exist (good) but there's no memory/CPU/disk quota.

**Fixed (approval gate):** `CodeSandboxExecutionWorker.run()` ([graphite_agents_code_sandbox.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_agents_code_sandbox.py)) now pauses on a `threading.Event` right after code is ready (hand-written or freshly generated) and before it touches the venv, pip, or the subprocess — it emits `approval_requested(code, requirements_manifest)` and blocks until the main thread calls `approve()` or `deny()`. `graphite_window_actions.py`'s `_handle_code_sandbox_approval_request` shows a modal confirmation naming the declared packages and the full code (via `setDetailedText`) before allowing the run to continue; declining (or stopping mid-wait) guarantees zero prepare/install/execute calls. Covered by [tests/test_code_sandbox_approval_gate.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/tests/test_code_sandbox_approval_gate.py).

**Still open:** the gate only adds a human checkpoint — it does not change what happens once approved. Real OS-level hardening (containerization, Job Objects, hash-pinned installs, resource quotas) is still recommended as a follow-up and was deliberately deferred pending a decision on adding new runtime dependencies (e.g. Docker) versus a stdlib-only approach.

### 2.2 Gitlink: the write-approval gate is UI-only, not structural — ✅ Fixed

[graphite_plugin_gitlink.py:1611-1656](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_gitlink.py#L1611) gated every disk write behind a single `QMessageBox.question` call — there was no separate "approved" flag or fingerprint on `pending_changes`; "approved" meant "whichever dict happened to be in `self.pending_changes` when the button was clicked." Path-traversal defenses (`_normalize_repo_path`, `_safe_local_target`, [lines 146-163](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_gitlink.py#L146)) were actually solid, but had **zero test coverage** despite being the entire security boundary for local writes.

**Fixed:** approval is now an explicit `DRAFT → PREVIEWED → APPROVED → APPLIED` state machine (`GITLINK_STATE_*` constants) with a `_fingerprint_changes()` hash stamped at confirmation time and re-verified immediately before any file is written — if the pending change set was mutated between the dialog and the write, the apply is refused instead of silently proceeding. `_normalize_repo_path`/`_safe_local_target` now have dedicated tests in [tests/test_gitlink_path_safety.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/tests/test_gitlink_path_safety.py), including Windows drive-letter and UNC-path injection cases.

---

## 3. Proposed Target Architecture

1. **A real `PluginSpec` registry.** Replace `PluginPortal._register_plugin`'s loose dict with a `PluginSpec` dataclass: `key, display_name, description, category, icon, node_cls, connection_cls, worker_cls, seedable: bool`. `PluginPortal` becomes a thin lookup over `PLUGIN_REGISTRY: dict[str, PluginSpec]` built once. `_node_label`, `WORKFLOW_PLUGIN_ICONS`/`WORKFLOW_ALLOWED_PLUGINS`, and graph_diff's node-label map all become `PLUGIN_REGISTRY[key].display_name` lookups instead of hand-copied dicts.
2. **A generic node-creation path.** One `PluginPortal.create_node(key, parent_node)` that uses `PluginSpec` fields to do the resolve/construct/position/addItem/list-append/connect dance generically, replacing the 13 near-identical `_create_X_node` methods. Per-plugin special cases (e.g. Graph Diff's two-source-node contract) become an explicit, small `SpecialSelection` hook rather than a bespoke method.
3. **Generic scene bookkeeping.** Replace the ~15 hardcoded `self.X_nodes`/`self.X_connections` list pairs and their 6x-repeated concatenation in `graphite_scene.py` with `self.plugin_nodes: dict[str, list]` / `self.plugin_connections: dict[str, list]` keyed by plugin key, and a single `all_plugin_nodes()` helper instead of the repeated concatenation chain.
4. **A `seed_prompt(text)` protocol method** on every plugin node, replacing the hardcoded isinstance chain in `_seed_plugin_prompt` ([graphite_window_actions.py:1655-1685](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_window_actions.py#L1655)) that currently must know each node type's private widget attribute name.
5. **Shared `graphite_plugins/common/` subpackage:**
   - `github_client.py` — a `GitHubRepoClient` used by both Code Review and Gitlink instead of two hand-rolled copies.
   - `llm_json_agent.py` — a base class for the JSON-fence-stripping / normalize / fallback-heuristic pattern shared by Code Review and Quality Gate.
   - `popup_combo.py` — Code Review's custom themed combo-box widget, generically reusable.
6. **Consolidate Code Sandbox** into `graphite_plugins/` (e.g. a `code_sandbox/` sub-package: `engine.py`, `agents.py`, `ui.py`) so the agents/plugins boundary confusion (§1.8) goes away.
7. ~~A single per-node worker-thread map on `main_window`... replacing the ad hoc single shared attribute (`self.sandbox_thread`, `self.quality_gate_thread`, `self.workflow_thread`, etc.)~~ **✅ Fixed, differently than proposed here.** Investigation found `QualityGateNode`/`CodeReviewNode`/`GitlinkNode`/`CodeSandboxNode` already had a proper per-node `node.worker_thread` attribute *alongside* the redundant `main_window.X_thread` singleton — so no new dict/map was needed, just deleting the dead singleton. `ArtifactNode`/`WorkflowNode`/`GraphDiffNode` had *only* the singleton with no per-node fallback, so they gained a `worker_thread` attribute matching the others' existing pattern. The singleton wasn't just dead weight for `sandbox_thread`: `stop_code_sandbox_node` actually called `.stop()` on whatever `main_window.sandbox_thread` currently was, so clicking "stop" on one sandbox node could stop a *different*, more-recently-started concurrent sandbox node's execution instead of (or as well as) its own — a real bug, not just redundant state. See §7 "Genuinely still open" follow-up work for detail.

None of this requires a big-bang rewrite — see §5 for a phased path that lets old and new plugins coexist during migration.

---

## 4. Per-Plugin Refactor Plans

### 4.1 Artifact / Drafter

**File:** [graphite_plugins/graphite_plugin_artifact.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_artifact.py) (604 lines)

**Overview:** Split-pane node for iteratively drafting a Markdown "living document" via chat instructions. Standard triad: `ArtifactConnectionItem`, `ArtifactNode` (bulk of the file), `ArtifactWorkerThread` wrapping a small `ArtifactAgent`.

**Weaknesses:**
1. ~~Dead vestigial code: `graphite_plugin_portal.py:352-354`'s `if not hasattr(scene, 'artifact_nodes'): ...` is unreachable~~ **✅ Fixed** — removed as part of the Phase 2 `create_node()` migration below; `graphite_scene.py:68/89` already unconditionally initializes both lists, so the check was always dead.
2. ~~`main_window.artifact_thread` is a single shared attribute, not per-node — a second concurrent Artifact node overwrites the first's thread reference with nothing tracking or cancelling the original.~~ **✅ Fixed** — `ArtifactNode` now has its own `worker_thread` attribute (matching Quality Gate/Code Review/Gitlink/Code Sandbox's existing pattern), and `graphite_window_actions.py` gained `stop_artifact_node`/`_cleanup_artifact_thread` using only the per-node reference. See §7.
3. ~~The "stop" icon shown by `set_running_state(True)` has no click handler wired to actually stop anything — purely cosmetic, implies a cancel capability that doesn't exist.~~ **Partially fixed**: `stop_artifact_node` now exists and correctly stops only this node's own thread, but it is **not yet wired to the stop icon's click handler** — that UI wiring is still open (the icon still does nothing when clicked; the capability now exists, it just isn't connected to the button yet).
4. `ArtifactAgent.get_response`'s tag-parsing regex ([~line 104-113](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_artifact.py#L104)) silently treats the entire raw LLM response as the new document body on any parse mismatch, fabricating a generic "I have updated the document" message — can silently corrupt the artifact. Still open.
5. Chat/preview rendering feeds arbitrary AI/user text through `markdown.markdown()` straight into `QTextEdit.setHtml` with no escaping ([~line 444](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_artifact.py#L444)); the resulting HTML is persisted and replayed verbatim on reload. Still open.
6. Deserializer reaches directly into `node.instruction_input`/`node.chat_display`/`node.chat_html_cache` rather than through an accessor — no model/view boundary. Still open.
7. Not exposed via the command palette (no reference found), unlike other discoverability paths. Still open.

**Refactor Plan:** Extract `ArtifactAgent` and tag-parsing into a standalone testable module with an explicit typed parse result (no silent full-text fallback); add unit tests for parsing edge cases. Track threads per-node (`main_window.artifact_threads: dict[node, thread]`) and wire the stop icon to a real cancel. Separate `ArtifactNode` into a thin Qt view plus a plain `ArtifactDocumentState` model so serializers stop poking widget internals directly. Sanitize/escape literal HTML before markdown rendering.

**Effort/Risk:** **M.** Thread-safety fix touches shared `graphite_window_actions.py` patterns used by sibling plugins. Model/view split is riskier because serializers depend on current attribute names — must preserve the on-disk schema while changing only the in-memory access path.

**✅ Phase 2 done:** `_create_artifact_node` in `graphite_plugin_portal.py` now delegates to the new generic `PluginPortal.create_node()` factory (see §4.8) instead of hand-rolling the parent-resolution/construction/positioning/scene-registration skeleton — this is where the dead `hasattr(scene, 'artifact_nodes')` check (weakness 1) got removed. Covered end-to-end by [tests/test_plugin_portal_create_node.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/tests/test_plugin_portal_create_node.py) (node/connection creation, scene registration, signal wiring, both warning-message paths). Weaknesses 2-7 are unrelated to the creation-path duplication and remain open.

**✅ Phase 4 done:** Artifact was the Phase 2 proof case; all 10 other standard single-parent plugins were migrated onto `create_node()` in Phase 4 (see §4.8) — this section's remaining weaknesses are unaffected either way.

---

### 4.2 Code Review Agent

**File:** [graphite_plugins/graphite_plugin_code_review.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_code_review.py) (2273 lines — largest plugin, >2x the average)

**Overview:** Reviews a local or GitHub source file with deterministic scoring, structured findings, and a weighted 8-category report (correctness, reliability, security, maintainability, readability, testing, performance, architecture) plus a verdict. Its size comes from three things bundled into one file: a ~290-line custom themed combo-box widget, a ~230-line hand-rolled GitHub REST client duplicated with Gitlink, and a full scoring/rubric engine duplicated in shape with Quality Gate.

**Weaknesses:**
1. ~~GitHub client (`_get_github_token`/`_github_headers`/`_github_request`) is near-duplicated with `graphite_plugin_gitlink.py`.~~ **✅ Fixed (Phase 3a)** — both now delegate to the shared `GitHubRestClient` (see §1.6).
2. ~~`CodeReviewAnalyzer` shares its exact JSON-fence-stripping and network-call skeleton with `QualityGateAnalyzer` (and, discovered in Phase 3b, `WorkflowArchitectAgent`) — copy-pasted, not factored.~~ **✅ Fixed for the boilerplate (Phase 3b)** — see §1.6; the scoring/normalization/fallback logic itself is still separate per plugin, which is correct (it's genuinely different rubric logic, not duplication).
3. `CodeReviewComboPopup`/`CodeReviewPopupComboBox` ([42-330](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_code_review.py#L42)) reimplement Qt combo-popup positioning purely for theming — ~290 lines with no counterpart elsewhere to share it.
4. Broad exception handling throughout (`get_response` [1056-1068](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_code_review.py#L1056), `load_local_file` [1756-1786](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_code_review.py#L1756), the GitHub load methods [1802-1921](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_code_review.py#L1802)) — network errors, JSON errors, and auth failures are all flattened into the same generic status string with no logging.
5. `CodeReviewWorkerThread.stop()` ([1094-1096](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_code_review.py#L1094)) can't interrupt an in-flight GitHub/LLM call already in `run()` — same systemic pattern as §1.7, worse here because these calls are the slowest in the app.
6. Scoring weights/thresholds are hardcoded as module globals and then duplicated a second and third time in prose inside markdown-builder strings ([356-384](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_code_review.py#L356), [393-441](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_code_review.py#L393), [824-826](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_code_review.py#L824)) — three places to keep in sync by hand.
7. `_fallback_review` ([853-1004](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_code_review.py#L853)) is a second, independent regex-heuristic scoring mechanism that silently substitutes for the LLM rubric on any exception, using different magic numbers than the primary rubric — the two can disagree on methodology while presenting an identical-looking report.
8. God-class: `CodeReviewNode` spans ~1140 lines mixing UI construction, GitHub networking, local file I/O, and badge rendering with no separation.
9. No tests anywhere for the scoring/normalization logic despite it being pure and cheap to test.

**Refactor Plan:** Extract the scoring engine into `graphite_plugins/code_review/scoring.py` (zero Qt imports, directly testable). Build the shared `common/github_client.py` (§3.5) and have both Code Review and Gitlink depend on it. Build the shared `common/llm_json_agent.py` base and have `CodeReviewAnalyzer`/`QualityGateAnalyzer` subclass it. Move the custom combo popup to `common/popup_combo.py`. Split `CodeReviewNode` into a thin view plus a `CodeReviewSourceController` (GitHub/local load) and a `CodeReviewReviewPresenter` (badge/report rendering). Centralize the scoring weights/thresholds in one place and generate the markdown descriptions from it instead of hand-duplicating.

**Effort/Risk:** **L** for the full split (largest, touches networking + a custom widget + scoring + view logic at once). An **M-sized, low-risk first slice**: extract just the GitHub client and scoring engine into pure-Python modules — both are self-contained and don't touch the widget layer. Biggest risk: `graphite_window_actions.py`, serializers, and deserializers all import `CodeReviewNode`/`CodeReviewConnectionItem` by name and consume its output dict keys directly (`review_markdown`, `quality_score`) — any refactor must preserve those shapes or update all dependents in lockstep.

**✅ Phase 3a/3b done (boilerplate)**: the GitHub-client half of the "low-risk first slice" above is complete (§1.6). The JSON-fence-stripping regex and network-call-then-parse boilerplate `CodeReviewAnalyzer` shared with `QualityGateAnalyzer` (and `WorkflowArchitectAgent`) is also extracted (§1.6).

**✅ Scoring engine extracted (not merged with Quality Gate's — see below):** `CodeReviewAnalyzer` and its ~700 lines of supporting constants/helpers (`REVIEW_CATEGORY_WEIGHTS`, `_fallback_review`'s heuristics, `_normalize_response`, markdown building, `_clean_text`/`_clamp_score`/`_severity_key`/etc.) moved to [graphite_plugins/code_review/scoring.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/code_review/scoring.py) — confirmed zero Qt imports (a test asserts this directly). `graphite_plugin_code_review.py` shrank from ~2270 lines to ~1550; the widget file now imports back the handful of utility functions (`_decode_text_bytes`, `_prepare_numbered_source`, `_is_reviewable_repo_path`, `_source_origin_label`, `_compact_label_text`, `CODE_REVIEW_METRIC_MARKDOWN`) that its own UI code (local/GitHub file loading, status labels) still needs, since those turned out to be shared between the rubric logic and the view rather than exclusive to either. `CodeReviewWorkerThread.agent` still holds a live `CodeReviewAnalyzer` instance exactly as before — construction, `SYSTEM_PROMPT`, and `get_response()` behavior are unchanged, only *where the code lives* changed. Covered by [tests/test_code_review_scoring.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/tests/test_code_review_scoring.py), which deliberately never creates a `QApplication` or imports the widget file, proving the module is genuinely standalone. **Not merged with `QualityGateAnalyzer`**: the two rubrics have different category weights, different fallback-heuristic scoring rules, and different normalization shapes — real domain logic, not incidental duplication (see §4.6's Refactor Plan for the same reasoning), so unifying them remains a separate, larger decision rather than something this extraction forces.

---

### 4.3 Execution Sandbox (Code Sandbox)

**Files:** [graphite_plugins/graphite_plugin_code_sandbox.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_code_sandbox.py) (860 lines, UI/view) + [graphite_agents_code_sandbox.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_agents_code_sandbox.py) (470 lines, root-level execution engine)

**Overview:** Runs a task description or hand-written Python inside a per-node, on-disk virtualenv with declared `requirements.txt` dependencies. Execution engine (`VirtualEnvSandbox`, `CodeSandboxExecutionWorker`, generation/repair LLM agents) lives entirely outside `graphite_plugins/`, inconsistent with every sibling plugin's self-contained layout.

**Weaknesses:** See §2.1 for the priority security findings (unconfined subprocess execution, unpinned/unverified pip installs). Beyond that:
1. ~~`main_window.sandbox_thread` is a single shared attribute, not per-node — concurrent sandbox nodes can clobber each other's thread reference.~~ **✅ Fixed.** This was a real, demonstrable bug, not just dead state: `stop_code_sandbox_node` called `.stop()` on whatever `main_window.sandbox_thread` currently was in addition to the correct per-node `sandbox_node.worker_thread` — so clicking "stop" on one sandbox node could also stop a different, more-recently-started concurrent sandbox node's execution. The shared attribute is now removed entirely; `stop_code_sandbox_node` only ever touches the target node's own `worker_thread`. Verified with two concurrent fake threads in [tests/test_per_node_worker_threads.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/tests/test_per_node_worker_threads.py).
2. No `worker_thread.wait()` before `deleteLater()` in cleanup ([graphite_window_actions.py:1057](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_window_actions.py#L1057)) or in `CodeSandboxNode.dispose()` — risk of a "destroyed while still running" condition under timing pressure.
3. `_is_error_output`'s naive keyword matching (e.g. `"exception:"` substring match) can misclassify a script that legitimately prints the word "Exception:" as a failure, triggering a wasted repair cycle.
4. Business logic living outside `graphite_plugins/` (§1.8) — UI file imports `SandboxStage` directly from the agents module with no interface boundary.
5. No tests for `sync_requirements`'s hash-caching logic or the attempt/repair loop.

**Refactor Plan:** Consolidate into `graphite_plugins/code_sandbox/` (`engine.py`, `agents.py`, `ui.py`). Introduce an `SandboxExecutionPolicy` (timeout, memory cap, package allowlist, network mode) as a prerequisite for real hardening. Remove the shared `main_window.sandbox_thread` singleton in favor of the per-node dict from §3.7. Add `wait()` calls before `deleteLater()`. Add tests for requirements-hash caching and error-output classification.

**Effort/Risk:** Consolidation + coupling cleanup: **M**. Thread-lifecycle fixes: **S**. Real sandbox hardening (containerization/Job Objects/pip verification): **L**, and should be scoped as its own security workstream per §2.1 rather than folded into this refactor's timeline.

---

### 4.4 Gitlink

**File:** [graphite_plugins/graphite_plugin_gitlink.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_gitlink.py) (1682 lines, down from 1884 — see item 5)

**Overview:** Fetches a GitHub repo (REST API or local checkout) into a stitched XML context bundle, sends it plus a task prompt to an LLM, parses a JSON change-set, renders a diff preview, and writes files to disk only after a confirmation dialog. Combines three external-facing concerns (GitHub API, local filesystem, LLM call) in one god-class.

**Weaknesses:** See §2.2 for the priority write-gate finding. Beyond that:
1. GitHub token is stored in plaintext at `~/.graphlink/session.dat` ([graphite_licensing.py:426-427,451-453](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_licensing.py#L426)) — consistent with how other API keys are handled in this codebase (systemic, not unique to Gitlink), but worth fixing alongside the write-gate work since both touch credential/trust boundaries.
2. `zipfile.extractall` on a downloaded GitHub zipball ([~1150-1179](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_gitlink.py#L1150)) has no signature/hash verification before extraction — low risk since the source is GitHub's own API, but worth noting.
3. Every GitHub call funnels through a bare `except Exception as exc: self.set_status(f"Error: {exc}")` pattern repeated ~6 times — no distinction between transient network errors and real failures.
4. `GitlinkWorkerThread.stop()` can't interrupt an in-flight blocking `api_provider.chat` call — same systemic pattern as §1.7.
5. ~~Path-safety helpers (`_normalize_repo_path`, `_safe_local_target`) are the entire security boundary for local writes and have **zero test coverage**.~~ **✅ Fixed (path-safety tests) and ✅ extracted (agent module).** Path-safety coverage landed first (`tests/test_gitlink_path_safety.py`, §2.2/§1.2). This pass went further: `GitlinkAgent` (the LLM change-proposal agent) plus its Qt-free supporting helpers (`_clean_text`, `_compact_label_text`, `_decode_text_bytes`, `_is_repo_text_path`, `_normalize_repo_path`, `_safe_local_target`, `_fingerprint_changes`, `_wrap_cdata`, `_xml_file_block`, `_truncate_for_context`, `_extract_json_object`, and the `TEXT_FILE_EXCLUSION_SUFFIXES`/`MAX_FILE_CONTEXT_CHARS` constants) moved to [graphite_plugins/gitlink/agent.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/gitlink/agent.py) — confirmed zero Qt imports (a test asserts this directly). `graphite_plugin_gitlink.py` shrank from 1884 to 1682 lines; the widget file imports back everything except `_wrap_cdata`/`MAX_FILE_CONTEXT_CHARS`/`TEXT_FILE_EXCLUSION_SUFFIXES` (those three are only used internally within the new module). `tests/test_gitlink_path_safety.py` was repointed at the new module location; `tests/test_gitlink_agent.py` (23 tests) covers the rest — text cleanup, XML formatting, and `GitlinkAgent.get_response`'s JSON-parsing/fallback paths — without creating a `QApplication` or importing the widget file. **`build_context_bundle`/`_resolve_scope_paths`/`_scan_local_repo_paths` deliberately did NOT move**, contrary to this refactor plan's original wording below: investigation (reading the actual methods, not just their names) showed they are `GitlinkNode` *instance methods*, not free functions — entangled with the local-root text field, the scope combo box, `get_selected_paths()`, the settings-manager-gated GitHub client, and on-disk snapshot downloads. Turning them into free functions would mean inventing a new parameter-passing contract that doesn't exist in the code today (a redesign, not a refactor) in exactly the security-sensitive, write-adjacent area where a subtly wrong redesign is a real risk rather than a mechanical gain. They stay as `GitlinkNode` methods, calling into the now-extracted pure helpers.

**Refactor Plan:** ~~Extract `gitlink_github_client.py` (shared with Code Review per §3.5).~~ **✅ Done (Phase 3a)** — see §1.6; `GitlinkNode` now delegates to `graphite_plugins/common/github_client.py`'s `GitHubRestClient`, same as Code Review. ~~Extract `gitlink_context_builder.py` (XML context assembly, pure functions, unit-testable independent of the node).~~ **✅ Done for the genuinely pure pieces** (item 5 above, as `graphite_plugins/gitlink/agent.py`) — **not done, deliberately, for `build_context_bundle` itself**, which turned out not to be a pure function once actually read (see item 5). Formalize the write gate as `gitlink_write_gate.py`: an explicit `DRAFT → PREVIEWED → APPROVED → APPLIED` state machine that fingerprints the exact file list shown to the user and refuses to apply unless the fingerprint still matches at write time — **✅ done in Phase 0**, see §2.2 (the `GITLINK_STATE_*` constants and the state-machine comment stayed in the widget file, since they describe `GitlinkNode`'s own attribute, not standalone logic).

**Effort/Risk:** **L** for the full modularization, **S** for the standalone write-gate + path-safety-test fix (recommended to do first, independent of everything else — see §2.2). Main breakage risk: session (de)serialization and portal instantiation both reach into `GitlinkNode` internals (`repo_state`, `context_xml`, `pending_changes`) directly, so any class-boundary change must preserve those attribute names/shapes.

---

### 4.5 Branch Lens (Graph Diff)

**File:** [graphite_plugins/graphite_plugin_graph_diff.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_graph_diff.py) (897 lines)

**Overview:** Compares two independently-selected branch-tip nodes and renders a side-by-side transcript plus an LLM-generated divergence summary. Structurally unusual: takes `left_source_node, right_source_node` instead of a single parent, and `self.parent_node` is unconditionally `None`. "Branch diff" is actually two independent linear walks up each side's `parent_node` chain (`_collect_branch_nodes`), not a real graph/tree comparison — there's no shared-ancestor detection, so a common prefix between the two branches is duplicated into both transcripts and sent to the LLM as if unrelated.

**Weaknesses:**
1. No shared-ancestor detection in the branch walk (see above) — semantically wrong for branches that share history, and wastes prompt budget.
2. Cycle guard (`while cursor and id(cursor) not in seen`) silently truncates on a cycle rather than logging/surfacing it — masks a scene-integrity bug elsewhere instead of failing loudly.
3. `_extract_node_text` is a ~90-line `hasattr`/`getattr` grab-bag enumerating ~15 unrelated per-plugin attribute names — every new sibling plugin field requires a manual update here, and it has no test coverage.
4. `valid_sources` tuple is hardcoded independently in `graphite_plugin_portal.py:487` — still a separate, independently-maintained list from the registry (still open). ~~This file's node-label map disagreed with `graphite_plugin_portal.py`'s registration~~ **✅ Fixed**: `_node_label` was its own hardcoded class-name → display-name map that was *missing `CodeReviewNode` entirely* — since `CodeReviewNode` is a valid Branch Lens comparison source, comparing a branch ending in a Code Review Agent node showed the raw class name "CodeReviewNode" in the transcript instead of "Code Review Agent". `_node_label` now delegates to `graphite_plugin_portal.get_display_name_for_node()` (PLUGIN_REGISTRY) for anything registered, keeping only a small residual map for non-plugin core node types (`ChatNode`). Required a deferred/local import inside `_node_label()` since `graphite_plugin_portal.py` imports this module at load time (to register Branch Lens) — importing it back at module level would be circular; this is a real structural constraint worth remembering for Phase 4 (any other plugin migrating its own hand-copied name map onto the registry will hit the same cycle and need the same deferred-import treatment, or a bigger registry-relocation refactor if it becomes common enough to be worth it). Covered by [tests/test_graph_diff_node_label.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/tests/test_graph_diff_node_label.py).
5. The two-node selection contract (`len(selected_nodes) != 2` at [graphite_plugin_portal.py:490](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_portal.py#L490)) gives no indication of which two nodes, and ordering is incidental to selection order rather than explicit.
6. Deserialization silently drops the whole node (and its connections) from the loaded scene if either source-node index fails to resolve — no warning, silent data loss on session load.
7. `GraphDiffAnalyzer.get_response` catches bare `Exception` and falls back silently with no logging — network failure and malformed-JSON failure look identical to the user.
8. `create_graph_diff_note`/`execute_graph_diff_node` in `graphite_window_actions.py` are near-verbatim copies of the Quality Gate equivalents, differing only in field names — a shared helper is conspicuously absent.

**Refactor Plan:** Extract `_collect_branch_nodes`, `build_branch_payload`, `_extract_node_text`, and `GraphDiffAnalyzer` into a Qt-free `graphite_plugins/graph_diff_core.py` for unit testing. Add real shared-ancestor detection to the branch walk. Introduce a shared `get_branchable_node_types()` used by both the portal and this file instead of two independently maintained lists. Log (and surface via notification banner) any source-index resolution failure during deserialization instead of silently dropping the node. Factor `execute_graph_diff_node`/`create_graph_diff_note` and their Quality Gate twins into one shared `run_llm_worker(...)` helper in `graphite_window_actions.py`.

**Effort/Risk:** **M.** Core extraction is mechanical and low-risk (the functions are already pure), but touches 6+ call sites. Biggest risk: changing `valid_sources` semantics and inadvertently allowing/disallowing a node type other plugins rely on for branch-tip selection.

---

### 4.6 Quality Gate

**File:** [graphite_plugins/graphite_plugin_quality_gate.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_quality_gate.py) (926 lines, down from 1512 — see item 3)

**Overview:** Walks the branch lineage back to the root, sends the flattened transcript to an LLM with a "production readiness" rubric, and renders a verdict, a 0-100 score, and up to 4 recommended follow-up plugins with seeded starter prompts (via `plugin_requested` → `instantiate_seeded_plugin`). Structurally the closest sibling to Code Review — both are "review/scoring" plugins with a near-identical analyzer shape.

**Weaknesses:**
1. ~~`_node_label` hardcodes the same 13 class-name → display-name pairs that the portal's registration already owns.~~ **✅ Fixed (Phase 5)** — migrated onto `graphite_plugin_portal.get_display_name_for_node()`, same pattern as Graph Diff's fix in §4.5 (including the same deferred-import requirement to avoid the portal↔plugin circular import). This map was already complete/correct (unlike Graph Diff's, which was missing `CodeReviewNode`), so this closed a duplication risk rather than a live bug. Covered by [tests/test_quality_gate_node_label.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/tests/test_quality_gate_node_label.py).
2. A second, independent allowlist/icon map (`QUALITY_GATE_PLUGIN_ICONS`/`QUALITY_GATE_ALLOWED_PLUGINS`) keyed by *display name* rather than class name — a third keying scheme for the same 9-13 plugins, all hand-maintained in this one file. **Deliberately left alone**: its icon values were checked against the registry and several differ on purpose or by prior drift (e.g. `"Py-Coder": "fa5s.code"` here vs. the registry's `"fa5s.laptop-code"`) — these icons drive the visible recommendation-card UI, so migrating them onto the registry would change what the user sees, not just refactor internal plumbing. That's a product decision, not a refactor, and out of scope here.
3. ~~`QualityGateAnalyzer` duplicates `CodeReviewAnalyzer`'s exact skeleton — same JSON-fence-stripping regex, same network-call boilerplate.~~ **✅ Fixed for the boilerplate (Phase 3b, §1.6/§4.2)**, and **✅ the analyzer itself extracted into its own Qt-free module**: `QualityGateAnalyzer` plus `build_quality_gate_payload`, `_node_label`, `_extract_node_text`, `_collect_branch_nodes`, `_flatten_content`, `_clean_text`, `_read_widget_text`, and the `QUALITY_GATE_PLUGIN_ICONS`/`QUALITY_GATE_ALLOWED_PLUGINS` maps moved to [graphite_plugins/quality_gate/scoring.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/quality_gate/scoring.py) — confirmed zero Qt imports (a test asserts this directly). `graphite_plugin_quality_gate.py` shrank from 1512 to 926 lines; the widget file imports back `QUALITY_GATE_PLUGIN_ICONS` (needed for the recommendation card's icon lookup) and `build_quality_gate_payload`/`QualityGateAnalyzer` (needed by `QualityGateNode`/`QualityGateWorkerThread`). `QUALITY_GATE_SCROLLBAR_STYLE` (pure Qt stylesheet text) deliberately stayed in the widget file rather than moving with the other module-level constants. Covered by [tests/test_quality_gate_scoring.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/tests/test_quality_gate_scoring.py), which — like Code Review's equivalent — never creates a `QApplication` or imports the widget file. **Still not merged with `CodeReviewAnalyzer`**: same reasoning as §4.2 — different category weights, different fallback-heuristic rules, different normalization shapes, real domain logic rather than incidental duplication. The fallback-heuristic-*scoring* logic itself (the actual point-adding/subtracting rules, item 4 below) is unchanged by this extraction — only *where the code lives* changed, not its behavior.
4. `_fallback_review`'s keyword-regex scoring adds/subtracts fixed point values (e.g. +12 for "has_code", -18 for "has_errors") based on substring matches over the whole transcript — trivially gamed by the literal word "test" appearing in prose; not real analysis. Still open — this is rubric-design work, not a mechanical refactor.
5. ~~`main_window.quality_gate_thread` is a single shared attribute, not per-node — same concurrency clobbering risk as Code Sandbox and Artifact.~~ **✅ Fixed** — removed; `QualityGateNode` already had its own `worker_thread` attribute doing the real per-node tracking, so the singleton was purely redundant dead state here (unlike Code Sandbox, nothing ever read it to make a decision). See §7.
6. `_setup_ui` is a single ~286-line method building the entire multi-tab widget tree, inline stylesheets, and badge logic. Still open — the extraction above deliberately only touched the Qt-free scoring engine, not the widget's own internal structure.
7. Bare `except Exception: pass` in `_read_widget_text` and in thread-signal disconnects during `dispose()` hides real errors. Still open.
8. ~~No tests anywhere for the scoring heuristics, payload-building, or JSON normalization, despite all of it being pure logic.~~ **✅ Fixed** — `tests/test_quality_gate_scoring.py` covers `_flatten_content`, `_clean_text`, `_collect_branch_nodes`, `_extract_node_text`, `build_quality_gate_payload`, and `QualityGateAnalyzer`'s fallback/get_response paths, now that the extraction makes them directly importable without Qt.

**Refactor Plan:** ~~Build the shared `common/llm_json_agent.py` base (§3.5) and have `QualityGateAnalyzer` and `CodeReviewAnalyzer` both subclass it instead of duplicating ~150 lines each.~~ **✅ Done** as plain shared functions (`common/llm_json.py`, §1.6/§3.5) rather than a subclassing base — see §4.2 for why. ~~Extract the rubric/fallback-scoring logic into a Qt-free module~~ **✅ Done** (`graphite_plugins/quality_gate/scoring.py`, item 3 above — not a separately-named `graphite_quality_rubric.py`, folded into the same module as the analyzer since payload-building and scoring are one cohesive unit, mirroring Code Review's structure). ~~Replace `_node_label` and the display-name allowlist with lookups against the registry from §3.1.~~ **✅ Done for `_node_label`** (Phase 5, item 1 above); the display-name *icon* allowlist remains deliberately separate (item 2). Split `_setup_ui` into smaller builder methods — still open (item 6). ~~Track threads per-node instead of the single shared attribute.~~ **✅ Done** (item 5, §7).

**Effort/Risk:** **M** (touches `graphite_plugin_portal.py`, `graphite_plugin_code_review.py`, `graphite_plugin_graph_diff.py`, and `graphite_window_actions.py` — 4-6 files). Risk is moderate: the JSON-normalization output must stay byte-for-byte compatible with persisted session fields (`readiness_score`, `verdict`, `recommendations` are reconstructed directly from saved data in `graphite_session/{serializers,deserializers}.py`), so schema drift during extraction could break old save-file loading.

---

### 4.7 Workflow Architect

**File:** [graphite_plugins/graphite_plugin_workflow.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_workflow.py) (1087 lines)

**Overview:** Takes a free-text goal/constraints, asks an LLM for a JSON execution blueprint, and renders recommended-plugin cards with "Add Node" buttons that seed new plugin nodes end-to-end via `plugin_requested` → `main_window.instantiate_seeded_plugin` → `PluginPortal.execute_plugin` (string match) → `_seed_plugin_prompt` (isinstance chain poking a starter prompt into whichever widget field that node type exposes). This is the most "meta" plugin in the codebase — it must know about every other plugin type to recommend/seed them, which is exactly where the missing registry hurts most (see §1.4's confirmed live bug).

**Weaknesses:**
1. ~~Triple-duplicated plugin enumeration in one file: `WORKFLOW_PLUGIN_ICONS`, `WORKFLOW_ALLOWED_PLUGINS` (derived from the same dict, at least not a fourth independent list), and a third copy embedded in the LLM system prompt text — none derived from the portal's registry.~~ **✅ Fixed for the system-prompt copy specifically** — `SYSTEM_PROMPT`'s "Allowed plugins:" bullet list is now generated from `WORKFLOW_ALLOWED_PLUGINS` via `_WORKFLOW_ALLOWED_PLUGINS_BULLETS = "\n".join(f"- {name}" for name in WORKFLOW_ALLOWED_PLUGINS)` and a plain `str.replace()` against a `__ALLOWED_PLUGINS_BULLETS__` marker (not an f-string/`.format()` on the whole prompt, since the JSON example later in the same string has literal `{`/`}` that would otherwise need escaping). This leaves exactly two independent things: `WORKFLOW_PLUGIN_ICONS` (a dict, source of truth) and `WORKFLOW_ALLOWED_PLUGINS` (`list(WORKFLOW_PLUGIN_ICONS.keys())`, derived, not hand-typed) — not three. Full unification onto the portal's `PLUGIN_REGISTRY` (so the icon map itself stops being separately maintained) is still open, same reasoning as Quality Gate's icon map in §4.6: the icon values intentionally diverge from the registry's in places, so that's a product decision, not a mechanical refactor. Covered by [tests/test_workflow_system_prompt.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/tests/test_workflow_system_prompt.py).
2. **Confirmed drift, fixed, and now structurally prevented** (§1.4): "Code Review Agent" was missing from the allowlist/icons/system-prompt/fallback-heuristics and has been added; item 1's generation fix means the system-prompt copy specifically can no longer drift out of sync with `WORKFLOW_ALLOWED_PLUGINS` again. "Branch Lens" remains intentionally excluded since it can't be seeded via the single-parent-plus-prompt contract this file uses (see §1.4) — a real fix needs a "requires pre-selected nodes" seeding path, not an allowlist edit.
3. `_normalize_plan` silently drops any LLM-recommended plugin not in the allowlist with a bare `continue` — no logging, no user-visible signal, falls through to a fully-generic fallback plan with no indication anything was filtered. Still open.
4. ~~`PluginPortal.execute_plugin`'s failure mode is `print()` + `None` — `instantiate_seeded_plugin` then silently no-ops; clicking "Add Node" visibly does nothing with no error.~~ **✅ Fixed** — see §1.1/§4.8; the not-found case now shows a notification banner instead of only printing to the console.
5. ~~`_seed_plugin_prompt` is a fourth hardcoded isinstance chain that must independently know each node type's private input-widget attribute name; any plugin missing from the chain silently drops the seed prompt.~~ **✅ Fixed** — see the Phase 2 note below; this no longer applies.
6. `WorkflowArchitectAgent.get_response` mixes prompt construction, network call, JSON parsing/repair, normalization, and markdown rendering in one method with a bare `except Exception` that masks the real failure reason. Still open.
7. No cancel affordance wired to `WorkflowWorkerThread.stop()` — nothing in `execute_workflow_node` ever calls it. Still open.
8. No tests for `_fallback_plan`'s keyword heuristics or `_normalize_plan`'s filtering. Still open (distinct from the system-prompt-generation tests added for item 1).

**Refactor Plan:** ~~Once the registry (§3.1) exists, derive `WORKFLOW_ALLOWED_PLUGINS`/icons/system-prompt text from `PluginPortal.get_plugins()` metadata instead of three hand-copied lists — this fixes §1.4's live bug as a side effect.~~ **✅ Done for the system-prompt text** (item 1) via generation from `WORKFLOW_ALLOWED_PLUGINS` directly, not by reaching into the portal's registry — simpler, and sufficient to close the actual drift bug, since the icon map's divergence-by-design (item 1) means full registry unification is a separate product decision anyway. Extract `WorkflowArchitectAgent` into a Qt-free module for unit testing — still open (unlike Code Review/Quality Gate/Gitlink's agents, not yet extracted). Make `_normalize_plan` return dropped-plugin names so the UI can surface "N recommendations were dropped" instead of failing silently — still open. ~~Make `PluginPortal.execute_plugin` raise a typed error (or return a result type) instead of `print`+`None`, and have `instantiate_seeded_plugin` show a notification banner on failure.~~ **✅ Done** — kept the `None` return (matching `create_node()`'s existing failure idiom, see §1.1) and added the notification banner directly inside `execute_plugin`, so every caller gets the fix for free rather than needing `instantiate_seeded_plugin` to add its own check. Replace the `_seed_plugin_prompt` isinstance chain with a `seed_prompt(text)` protocol method implemented per plugin node (§3.4) — **✅ done**, see below.

**Effort/Risk:** **M** for the full plan; **S** if only fixing the immediate drift bug (adding the two missing names to the three lists) plus adding failure logging — this is what actually got done for items 1/2/4. The `seed_prompt` protocol migration was the riskiest single piece — it touches every plugin node class and must preserve existing side effects (e.g. some nodes also call an `_on_X_changed()` callback after setting text) exactly, or a seeded prompt silently stops working for one node type; already done and verified (see Phase 2 note below).

**✅ Phase 2 done (seed_prompt):** All 11 seedable node classes now implement `seed_prompt(text)` themselves, reproducing the prior per-type widget-and-side-effect behavior exactly (verified with real, headlessly-constructed node instances, not just source inspection — see [tests/test_seed_prompt_protocol.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/tests/test_seed_prompt_protocol.py)). `_seed_plugin_prompt` in `graphite_window_actions.py` is now a two-line dispatcher: call `node.seed_prompt(text)` if present, else fall back to the `Note`-specific branch (System Prompt has no dedicated node class). Adding a new seedable plugin no longer requires touching this file at all. **✅ Also done this pass:** the system-prompt copy of the allowlist enumeration (weakness 1) and the non-typed `execute_plugin` failure mode (weakness 4). **Still open in this section:** full icon-map unification onto the registry (a deliberate product decision, not a refactor - see item 1), extracting `WorkflowArchitectAgent` into its own module (unlike its Code Review/Quality Gate/Gitlink counterparts), `_normalize_plan`'s silent-drop behavior (weakness 3), and cancel support for `WorkflowWorkerThread` (weakness 7).

---

### 4.8 Plugin Portal (meta-infrastructure)

**File:** [graphite_plugins/graphite_plugin_portal.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_portal.py) (614 lines)

**Overview:** This is the file that should be the plugin system, and isn't (see §1.1). It also directly imports every other plugin module ([lines 3-19](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_portal.py#L3)) and contains 13 hand-written factory methods.

**Weaknesses:** All of §1.1-§1.4 live here. Additionally: `_resolve_branch_parent` special-cases `CodeNode` ([line 229](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_portal.py#L229)) with no documented reason why that one node type needs different parent resolution — an undocumented special case future plugin authors won't know to replicate or avoid. `PLUGIN_CATEGORY_META` ([22-48](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_portal.py#L22)) is a separate hardcoded list that must stay in sync with the `category=` string used in each `_register_plugin` call, with a silent "More Plugins" catch-all bucket if they ever drift ([194-201](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_portal.py#L194)) — a drift-tolerant fallback that hides the bug instead of surfacing it.

**Refactor Plan:** This is the centerpiece of §3 — implement `PluginSpec`/`PLUGIN_REGISTRY` and the generic `create_node(key, parent_node)` path here. Once done, this file should shrink from ~614 lines of hand-written factories to a short declarative table plus one generic method with named hooks for the handful of genuinely special cases (Graph Diff's two-source selection, System Prompt's note-based implementation).

**Effort/Risk:** **L**, but additive — the registry and generic path can be built and tested alongside the existing hardcoded methods, then each `_create_X_node` migrated one at a time (see §5), so this doesn't require a single risky cutover.

**✅ Phase 1 done:** `PluginSpec` (dataclass) and a module-level `PLUGIN_REGISTRY: dict[str, PluginSpec]` now exist in this file, populated for all 13 plugins with `key`/`display_name`/`description`/`category`/`icon`/`node_cls`/`connection_cls`/`seedable`. `get_plugin_spec(key)` and `get_display_name_for_node(node_or_cls)` are available as the intended eventual replacement for the hand-copied name maps in `graphite_plugin_quality_gate.py`/`graphite_plugin_graph_diff.py`/`graphite_plugin_workflow.py` — wired into `graphite_plugin_graph_diff.py` in Phase 2 (see §4.5), the other two are still open. `_discover_plugins` and 12 of the 13 `_create_X_node` factories are untouched. [tests/test_plugin_registry.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/tests/test_plugin_registry.py) cross-checks the registry against the live `PluginPortal` registration (name drift) and against the `seedable` flag vs. each node class actually implementing `seed_prompt()` — the same class of drift that caused the Workflow allowlist bug in §1.4, now caught by a test instead of a future audit. As a side effect, `ArtifactNode`/`ArtifactConnectionItem` moved from a deferred import inside `_create_artifact_node` to a top-level import (no circular-import reason existed for the deferral, confirmed before the change).

**✅ Phase 2 done:** Added `PluginPortal.create_node()` — a generic single-parent factory that handles the ~15-line skeleton duplicated across most `_create_X_node` methods (resolve/validate parent, construct, wire into `children`, optionally clone history, position, register node + connection with the scene's still-hardcoded lists — see §3.3, not yet touched). `_create_artifact_node` is migrated onto it as the first proof (and shrank from ~30 lines to a single `create_node(...)` call, also removing the dead `hasattr(scene, 'artifact_nodes')` check flagged in §4.1). The other 12 factories are intentionally **not** migrated yet — one at a time, per §5. `scene_nodes`/`scene_connections` are passed as the actual list objects rather than derived from the plugin key, because the naming isn't consistent (e.g. the "html_renderer" plugin's list is `scene.html_view_nodes`, named after the class instead) — generalizing that is left to §3.3's own follow-up rather than papered over here. Covered by [tests/test_plugin_portal_create_node.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/tests/test_plugin_portal_create_node.py). The `seed_prompt` protocol (§3.4) was implemented for all 11 seedable plugins, not just the two named for this phase, since it was no more work to do broadly than narrowly and immediately removed the isinstance chain's maintenance burden everywhere at once — see §4.7 below and the standalone commit for detail. Per-node scene bookkeeping (§3.3) was evaluated and deliberately deferred: it touches `graphite_scene.py` in 7+ places with no existing test coverage and no way to interactively verify the running GUI in this environment, so it's a poor fit for incremental, verifiable progress right now — recommended as its own focused phase once broader test coverage or interactive verification is available.

**✅ Phase 4 done (create_node() migration, not the isinstance-chain deletion — see below):** the remaining 10 standard-shape factories (Py-Coder, Execution Sandbox, Workflow Architect, Quality Gate, Code Review Agent, Gitlink, Graphlink-Web, Conversation Node, Graphlink-Reasoning, HTML Renderer) are all migrated onto `create_node()`. 11 of 13 plugins now share the one factory; only System Prompt (note-based, attaches to the graph root) and Branch Lens (two-pre-selected-nodes contract) keep bespoke factories, both for the genuine structural reasons already documented rather than any migration difficulty. `create_node()` gained one small addition to support this: an optional `validate_parent` predicate (HTML Renderer only accepts a specific tuple of node types, not just "has a `children` attribute") — still "one parent, one new node," just a different acceptance test, so it didn't warrant a separate code path. Every plugin's exact prior behavior was preserved and verified individually: signal wiring, conditional history cloning (and Conversation Node's different `set_history()`-based cloning, which doesn't fit the generic `clone_parent_history` flag and stays in its own `wire` closure), `settings_manager` injection for Code Review/Gitlink, and `_resolve_branch_parent`'s CodeNode-redirect behavior. Covered by [tests/test_plugin_portal_remaining_factories.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/tests/test_plugin_portal_remaining_factories.py) (33 tests).

**Bug discovered during this migration (pre-existing, confirmed present in the pre-refactor baseline, not introduced here) — ✅ Fixed in Phase 5:** HTML Renderer's `valid_parents` tuple includes `CodeNode`, and the factory is meant to copy `CodeNode.code` into the new `HtmlViewNode` (`if isinstance(selected_node, CodeNode): html_view_node.set_html_content(selected_node.code)`) — but `CodeNode` has no `children` attribute, and the unconditional `parent_node.children.append(node)` step ran *before* that copy step, so selecting a `CodeNode` and adding an HTML Renderer raised `AttributeError` instead of copying the code. Left unfixed when first discovered (the obvious fix risked trading a loud crash for a silent orphaned-node leak if `.children` was needed for deletion cascades elsewhere). A dedicated investigation in Phase 5 confirmed `.children` is never used for deletion or connection-validity discovery in `graphite_scene.py` — only for branch-visibility traversal, a system `CodeNode` never participated in — so `create_node()`'s `children.append()` step is now guarded with `hasattr(parent_node, 'children')`. Selecting a `CodeNode` and adding an HTML Renderer now succeeds and copies the code as intended. The regression test that previously asserted the crash now asserts the fix.

**Still not done — and deliberately separate from the above:** deleting the corresponding hardcoded `isinstance()` chains and per-type scene list pairs in `graphite_scene.py`/`graphite_window.py`/`graphite_window_actions.py`/session files. Migrating a plugin's *creation* onto `create_node()` doesn't require touching those chains at all — they serve rendering, serialization, and deletion, not creation — so this was correctly left decoupled rather than bundled in. That cleanup is real, separate work with the same higher risk profile called out for §3.3's scene-bookkeeping generalization (7+ touch points per plugin, no existing test coverage there, no interactive GUI verification available in this environment) and is recommended as its own phase.

---

### 4.9 Plugin Flyout / Picker + Context Menu

**Files:** [graphite_plugins/graphite_plugin_picker.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_picker.py) (410 lines), [graphite_plugins/graphite_plugin_context_menu.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/graphite_plugins/graphite_plugin_context_menu.py) (92 lines — ✅ moved into the package, was root-level)

**Overview:** `PluginFlyoutPanel` is the popup UI that lists categories/plugins from `PluginPortal.get_plugin_categories()` and emits `pluginSelected`. `PluginNodeContextMenu` is the shared right-click menu ("Open Document View" / "Collapse" / "Delete") used by every plugin node via `getattr`-based capability detection (`supports_branch_context_toggle`, `toggle_collapse`).

**Weaknesses:** This is the healthiest part of the plugin system — it's a genuinely generic, data-driven UI that iterates `get_plugin_categories()`/`get_plugins()` rather than hardcoding per-plugin widgets, and the context menu's `getattr`-based capability checks are a reasonable lightweight duck-typing pattern (arguably a model for what §3.4's `seed_prompt` protocol should look like elsewhere). ~~The only real issue: `graphite_plugin_context_menu.py` lives at the repo root while everything that uses it lives in `graphite_plugins/`.~~ **✅ Fixed.**

**Refactor Plan:** ~~Move `graphite_plugin_context_menu.py` into `graphite_plugins/graphite_plugin_context_menu.py`.~~ **✅ Done**, and it surfaced a real bug beyond the plan's own prediction: `graphite_plugins/__init__.py` eagerly imported every plugin submodule, so moving a file with *no* plugin-specific dependencies into the package still triggered the full plugin dependency graph on import — which circled back to `graphite_pycoder.py` (a dependency of `graphite_connections.py`, itself a dependency of every plugin module) via the very module trying to import `PluginNodeContextMenu`. Fixed by emptying `graphite_plugins/__init__.py`'s re-exports (confirmed unused by every actual consumer first) rather than reordering or deferring the move-triggering import — see §1.5 and §5.

**Effort/Risk:** ~~**S.** Pure file-move plus import-path updates; low risk since behavior doesn't change.~~ Turned out **M** in practice — the file move itself was trivial, but it surfaced the circular-import bug above, which needed its own investigation and fix.

---

## 5. Suggested Migration Sequencing

Doing all of this at once would be its own brittle, high-risk rewrite. Recommended phases, each independently shippable:

1. **✅ Phase 0 (done):** the two priority fixes in §2 — Code Sandbox's human-approval gate and Gitlink's write-gate state machine + path-safety tests. The Workflow allowlist drift (§1.4/§4.7) is fixed for Code Review Agent; Branch Lens's exclusion needs the seeding-flow enhancement described in §4.7, not a quick list edit (correctly left alone, not a bug).
2. **✅ Phase 1 (done, additive):** built `PluginSpec`/`PLUGIN_REGISTRY` in `graphite_plugin_portal.py` (§3.1) alongside the existing hardcoded `_create_X_node` methods — nothing deleted.
3. **✅ Phase 2 (done, adjusted from the original plan):** built the generic `create_node()` path (§3.2) and migrated `_create_artifact_node` onto it — Artifact was the clean "single parent" proof case. Graph Diff was **not** migrated onto `create_node()`: its two-pre-selected-node creation contract doesn't fit the single-parent shape, and forcing it through would have meant either warping the generic path around one exception or building a second, parallel abstraction for one plugin — neither is worth it for a single case (see §4.5, §4.8). Instead, Graph Diff's `_node_label` hand-copied map was migrated onto `get_display_name_for_node()`, which is the part of "onto the registry" that actually applied to it, and fixed a real bug in the process (missing `CodeReviewNode` entry). The `seed_prompt` protocol (§3.4) was implemented for **all 11** seedable plugins rather than just the two named here, since scoping it down would have meant doing the same isinstance-chain analysis twice. Per-node scene bookkeeping (§3.3) was evaluated and **deliberately deferred** — see §4.8's Phase 2 note for the reasoning (high blast radius in `graphite_scene.py`, no test coverage there yet, no interactive GUI verification available in this environment).
4. **✅ Phase 3 (shared engines) done, scope adjusted from the original plan:** extracted `common/github_client.py` (§3.5) — Code Review and Gitlink both migrated onto it (§1.6). Extracted `common/llm_json.py` (renamed from the originally-proposed `llm_json_agent.py`, and built as plain functions rather than a base class — see §1.6 for why): `extract_json_object` is used by Code Review, Quality Gate, Workflow (discovered during this phase, not in the original plan), and Gitlink — all four independently hand-rolled the same regex. `call_llm_and_parse_json` is used by Code Review, Quality Gate, and Workflow, which additionally shared the exact network-call-then-parse shape around that regex. Gitlink's `GitlinkAgent.get_response` was deliberately **not** migrated onto `call_llm_and_parse_json` — its network call sits outside its try/except entirely, a real behavioral difference from the other three, not an oversight to paper over. Each analyzer's actual scoring/rubric/normalization logic remains separate and un-factored — extracting *that* is materially riskier and is left for a dedicated future pass, not bundled into this phase.
5. **✅ Phase 4 (create_node() migration) done, isinstance-chain deletion deliberately excluded from scope:** all 10 remaining standard-shape factories (Py-Coder, Execution Sandbox, Workflow Architect, Quality Gate, Code Review Agent, Gitlink, Graphlink-Web, Conversation Node, Graphlink-Reasoning, HTML Renderer) are migrated onto `create_node()` — see §4.8. This turned out to be separable from "delete that plugin's entries from the hardcoded isinstance chains in `graphite_scene.py`/etc." from the original plan: those chains serve rendering/serialization/deletion, not creation, so migrating *how a node gets created* didn't require touching them, and doing so was correctly left out rather than bundled in and taking on its higher risk profile unnecessarily. A pre-existing HTML Renderer bug (crashes on a `CodeNode` parent before its content-copy feature runs) was discovered and documented, not fixed, during this pass. The portal↔plugin circular-import constraint from §4.5 didn't recur here since none of these 10 factories needed to consume the registry from their own module (only `_node_label`-style metadata lookups do, and none of the 10 had one) - Graph Diff and (if ever migrated) Quality Gate/Workflow's own hand-copied name maps remain the only places this would bite.
6. **✅ Phase 5 (cleanup) done**, except the isinstance-chain deletion/full scene-bookkeeping rewrite, which was investigated in depth and found to be a poor fit for this pass (see below):
   - ✅ Migrated Quality Gate's `_node_label` onto the registry (§4.6), same pattern and same circular-import constraint as Graph Diff's §4.5 fix. Workflow has no equivalent pure-text label function to migrate.
   - Quality Gate's and Workflow's icon/allowlist maps (`QUALITY_GATE_PLUGIN_ICONS`/`WORKFLOW_PLUGIN_ICONS`) were evaluated and **deliberately not migrated** — their icon values diverge from the registry's on purpose or by prior drift, so unifying them would visibly change the recommendation-card UI rather than just refactor internal plumbing; that's a product decision for the user to make, not something to fold into a behavior-preserving refactor.
   - ✅ **Fixed the HTML Renderer/`CodeNode` bug** from §4.8: a dedicated investigation (spawned specifically to answer this) confirmed `.children` is *not* used to discover nodes for deletion or connection-validity anywhere in `graphite_scene.py` — deletion iterates `scene.selectedItems()` directly and looks each node up in its own per-type scene list; `.children` is only used for branch-visibility traversal, a system `CodeNode` never participated in anyway. `PluginPortal.create_node()`'s `parent_node.children.append(node)` is now guarded with `hasattr(parent_node, 'children')`, so selecting a `CodeNode` and adding an HTML Renderer now succeeds and copies the code content as originally intended, instead of crashing. Verified with a real `ArtifactNode`/`CodeNode` instance, not a mock.
   - ✅ **Resolved the dual import paths** (§1.5): all 7 core files that imported plugin classes via the old root-level shim modules (`graphite_scene.py`, `graphite_window.py`, `graphite_window_actions.py`, `graphite_window_navigation.py`, `graphite_session/{serializers,deserializers,scene_index}.py`) now import from `graphite_plugins.graphite_plugin_X` directly, including one deferred import inside `graphite_window_actions.py` that the initial grep sweep missed (anchored searches don't catch indented function-body imports). The 9 now-unused root shim files (`graphite_plugin_artifact.py` etc.) were deleted.
   - ✅ **Moved `graphite_plugin_context_menu.py` into the package** (§4.9) as `graphite_plugins/graphite_plugin_context_menu.py`, with all 12 importers updated. This surfaced a real circular import: `graphite_plugins/__init__.py` used to eagerly import every plugin submodule, so importing anything from the package — even `PluginNodeContextMenu`, which has no plugin-specific dependencies — pulled in the full plugin dependency graph, which circled back through `graphite_connections.py` to `graphite_pycoder.py`, the very module doing the import. Fixed by making `graphite_plugins/__init__.py` import nothing: confirmed first that no code anywhere actually used its re-exports (every real consumer already imports from the specific submodule it needs), so this was a safe simplification, not a compromise. Covered by [tests/test_import_chain.py](C:/Users/Admin/source/repos/graphite_app/graphite_app/tests/test_import_chain.py), which pins both the import chain and the now-empty package `__init__.py` as regression guards.
   - ✅ **Partially addressed §3.3** (generic scene bookkeeping): a dedicated investigation mapped every multi-plugin concatenation and isinstance chain in `graphite_scene.py`, `graphite_window.py`, `graphite_window_actions.py`, and the three session files. Two findings shaped what was and wasn't done:
     1. `graphite_scene.py` already had unused `_all_conversational_nodes()`/`_all_content_nodes()`/`_all_layout_nodes()` helper methods (a union of the 12 plugin lists, content-node lists, and both, respectively) that just weren't consistently called — five methods (`find_items`, `update_search_highlight`, `add_chat_node`'s parent validation, `nodeMoved`'s type validation, `update_connections`) still built the same unions inline. All five now call the existing helpers instead of repeating the 12-plugin-list concatenation, verified via a real `ChatScene` instance (constructible headlessly) with real node instances, not fakes. This means adding a 13th plugin now requires updating the three helpers instead of eight inline expressions.
     2. The deletion isinstance chain in `graphite_scene.py::deleteSelectedItems` and the various node-type tuples in `graphite_window.py`/`graphite_window_actions.py` were **not** unified: direct reading (not just the investigation summary) showed real per-type differences — only some node types call `dispose()`, only three (`QualityGateNode`/`CodeReviewNode`/`GitlinkNode`) have extra worker-thread cleanup and `current_node`-clearing logic, and `GraphDiffNode` has an entirely bespoke deletion path (`_delete_graph_diff_node`) since it has no single parent connection. Collapsing this into one generic dict-driven loop would require encoding all of that per-type metadata just to save a few lines per branch, with correctness risk (thread leaks, wrong dispose behavior) that can't be verified interactively in this environment. Left as-is rather than force a risky generalization for a small mechanical gain.

This ordering front-loads the two genuine security/safety risks, proves the new architecture on low-complexity plugins before betting the complex ones on it, and keeps every intermediate state shippable.
